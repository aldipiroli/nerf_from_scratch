CKPT_DIR: "../artifacts/ckpts"
LOG_DIR: "../artifacts/logs"
IMG_OUT_DIR: "../artifacts/img"
MODEL:
  input_size: 6
  output_size: 4
  embed_size: 64
DATA:
  root: "../data"
  batch_size: 8
  N: 1000 # num points in image plane
  M: 32 # num query points per ray
  tn: 2 # near distance ray
  tf: 6 # far distance ray
  H: 100
  W: 100
OPTIM:
  optimizer: "AdamW"
  lr: 0.001
  weight_decay: 0.0001
  weight_decay_step: 50
  num_epochs: 100000
  eval_every: 5
